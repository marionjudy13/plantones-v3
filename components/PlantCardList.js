import PlantCard from "./PlantCard";

export default function PlantCardList({ props }) {
    return (
        <section class="border border-green-400 hidden lg:block">  
            This is a list of plants.
        </section>
    )
}